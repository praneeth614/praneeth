git clone git@github.com:kanupra01/twinkle.git
cd twinkle


git add .
git commit -m "any message"
git push origin master

git branch -a# list branches
git checkout -b pranneth #creates new branch along master code

git add .
git commit -m "any message"
git push origin pranneth


#sync new branch code
commit first then
git pull 
git push origin branch


git checkout -b branchname

